{
  "name": "freelanceflow",
  "version": "1.0.0",
  "description": "FreelanceFlow — full-stack freelancer management app",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/server.js",
    "dev:server": "cd server && node server.js",
    "dev:client": "cd client && npm run dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.0",
    "multer": "^2.1.1"
  }
}
